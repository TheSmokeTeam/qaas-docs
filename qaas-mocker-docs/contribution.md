# Contribution

1. Clone this repository.
2. Open `qaas-mocker-docs`.
3. Start the local site with `mkdocs serve`.
4. Edit the markdown files under `/docs/`.
5. Build with `mkdocs build` before opening a PR.

# OracleSqlDataBaseTablesTruncate Configurations Yaml View

> TL;DR: Minimal YAML scaffold for OracleSqlDataBaseTablesTruncate ProbeConfiguration — copy, fill the blanks, drop into a Session step.

Use this generated scaffold as the starting point for the hook configuration block.

## Minimal example

```yaml
ProbeConfiguration:
  ConnectionString: "value"
  CommandTimeoutSeconds: "${value}"
  UseGlobalDict: "${value}"
  TableNames:
    - "value"
```

## See also

Use the surrounding documentation navigation to move between related generated reference pages.

# EmptyElasticIndices Configurations Yaml View

> TL;DR: Minimal YAML scaffold for EmptyElasticIndices ProbeConfiguration — copy, fill the blanks, drop into a Session step.

Use this generated scaffold as the starting point for the hook configuration block.

## Minimal example

```yaml
ProbeConfiguration:
  IndexPattern: "value"
  Password: "value"
  Url: "value"
  Username: "value"
  AllowInvalidServerCertificates: "${value}"
  MatchQueryString: "*"
  RequestTimeoutMs: "${value}"
  UseGlobalDict: "${value}"
```

## See also

Use the surrounding documentation navigation to move between related generated reference pages.

# DataSourceResponseProcessor Configurations Yaml View

> TL;DR: Minimal YAML scaffold for DataSourceResponseProcessor ProcessorConfiguration — copy, fill the blanks, drop into a Session step.

Use this generated scaffold as the starting point for the hook configuration block.

## Minimal example

```yaml
ProcessorConfiguration:
  SelectionMode: "First"
  StatusCode: "${value}"
  ContentType: "value"
  DataSourceName: "value"
  FallbackBody: "value"
  Index: "${value}"
  ResponseHeaders: "${value}"
```

## See also

Use the surrounding documentation navigation to move between related generated reference pages.

# DeleteS3Bucket Configurations Yaml View

> TL;DR: Minimal YAML scaffold for DeleteS3Bucket ProbeConfiguration — copy, fill the blanks, drop into a Session step.

Use this generated scaffold as the starting point for the hook configuration block.

## Minimal example

```yaml
ProbeConfiguration:
  AccessKey: "value"
  SecretKey: "value"
  ServiceURL: "value"
  StorageBucket: "value"
  ForcePathStyle: "${value}"
  UseGlobalDict: "${value}"
```

## See also

Use the surrounding documentation navigation to move between related generated reference pages.

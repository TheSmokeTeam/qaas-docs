# mkdocs nav fragment — Catalogs worktree contribution
# Merged into root mkdocs.yml by docs-integration agent.
#
# Adds:
#   - 4 family landing pages (index.md placeholders; refreshed later)
#   - 4 schema reference pages auto-generated by generate-schema-doc
# Does NOT add per-plugin pages (216) — those already live under
# the existing assertions/generators/probes/processors trees.

nav_additions:

  - section: Assertions
    parent_path: ["Runner", "Plugin Catalogs"]
    items:
      - Overview: assertions/index.md
      - Schema Reference: _generated/schemas/assertions.md

  - section: Generators
    parent_path: ["Runner", "Plugin Catalogs"]
    items:
      - Overview: generators/index.md
      - Schema Reference: _generated/schemas/generators.md

  - section: Probes
    parent_path: ["Runner", "Plugin Catalogs"]
    items:
      - Overview: probes/index.md
      - Schema Reference: _generated/schemas/probes.md

  - section: Processors
    parent_path: ["Runner", "Plugin Catalogs"]
    items:
      - Overview: processors/index.md
      - Schema Reference: _generated/schemas/processors.md

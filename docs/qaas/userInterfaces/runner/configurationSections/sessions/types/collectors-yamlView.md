# Collectors Configurations Yaml View

```yaml
Collectors:
  - Name:
    Configuration:
    EndTimeReachedCheckIntervalMs:
    CollectionRange:
      EndTimeMs:
      StartTimeMs:
    DataFilter:
      Body:
      MetaData:
      Timestamp:
    Prometheus:
      Expression:
      Url:
      ApiKey:
      SampleIntervalMs:
      TimeoutMs:
```
